# Troubleshooting

## Common issues

- Missing FFmpeg
- Python package import errors
- No face detected in portrait image
- Audio shorter than the minimum enrollment length
- SQLite path permissions

## Notes

The scaffold intentionally keeps model loading lazy so the app can start before all AI dependencies are available.

